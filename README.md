# Chessalatro
A chess-like strategy game with shop and roster mechanics.

## Setup
1. Clone the repository.
2. Run `npm install` to install dependencies.
3. Run `npm start` to launch the game.
4. Run `npm test` to execute tests.

## Status
Refactored with modular architecture. Core, UI, and game loop implemented.